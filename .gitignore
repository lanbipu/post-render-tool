__pycache__/
*.py[cod]
.DS_Store
*.DS_Store

# Claude Code superpowers worktrees — 不应进入 P4 graph depot
.claude/worktrees/

# Claude Code 本地 scheduled task 调度锁 — 运行时 state, 不入仓
.claude/scheduled_tasks.lock

# 多版本打包产物
releases/

# Codex CLI / companion script transient logs (含 ChatGPT auth metadata)
error.log
.venv_*
